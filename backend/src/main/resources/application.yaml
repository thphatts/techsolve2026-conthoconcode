spring:
  application:
    name: FridgeAI

  # Cấu hình MySQL
  datasource:
    url: jdbc:mysql://localhost:3306/fridgeai_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: thphat08122007 # Mật khẩu XAMPP/MySQL của bạn

  jpa:
    # ddl-auto: update LÀ CHÌA KHÓA CHO HACKATHON
    # Nó sẽ tự động tạo bảng dựa trên class @Entity của bạn, bạn không cần viết SQL bằng tay!
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format-sql: true

  # Cấu hình JWT Token cho Authentication
  jwt:
    secret: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970" # Khóa bí mật 256-bit Base64
    expiration: 86400000 # Thời gian hết hạn của token: 1 ngày (tính bằng milliseconds)

  # Thêm phần này vào dưới cùng của application.yml
app:
  ai:
    # Tạm thời bạn có thể để một chuỗi bất kỳ để app chạy lên,
    # sau này có key thật từ Google AI Studio thì paste vào đây
    api-key: "AIzaSyAHHYVzEoNzCdjxrzdcfwp_3OIjRMB6zjY"
    model: "gemini-pro"
    bill-scan-prompt: "Bạn là một chuyên gia đọc hóa đơn siêu thị. Hãy đọc ảnh hóa đơn này và trả về JSON chứa mảng 'items' gồm các trường: product_name (tên món hàng), quantity (số lượng), price (giá một đơn vị), estimated_expiry_days (số ngày hết hạn ước tính dựa trên loại đồ ăn, ví dụ thịt là 3-5 ngày, rau là 5-7 ngày). Chỉ trả về JSON hợp lệ."
